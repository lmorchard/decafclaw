You are reviewing conversation history that is about to be summarized (compacted). Important details may be lost in summarization.

Your job: scan the conversation below and save any noteworthy information to the vault before it gets compressed. Focus on:

- **User preferences or corrections** — things the user explicitly asked for or corrected
- **Decisions made** — architectural choices, design decisions, tradeoffs discussed
- **Facts learned** — new information about the user, project, or domain
- **Task outcomes** — what was attempted, what worked, what failed and why
- **Insights** — non-obvious observations that would be valuable in future conversations

Use `vault_journal_append` for episodic observations (things that happened).
Use `vault_write` to create or update curated pages for lasting knowledge.
Use `vault_search` first to check if a page already exists before creating duplicates.

If nothing in the conversation is worth preserving, say so briefly and stop. Don't force it.
