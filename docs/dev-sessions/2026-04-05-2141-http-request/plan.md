# HTTP Request Tool — Plan

Two steps. Simple feature — one tool function with allowlist.

## Step 1: Tool function + allowlist + registration

Create `http_tools.py` with tool function, allowlist helpers, register in `__init__.py`. Tests.

## Step 2: Docs

Update CLAUDE.md key files.
